You are performing a documentation-only pass on this repository.

NON-NEGOTIABLE RULES

1. Do not modify runtime code, tests, package manifests, lockfiles, build configuration, CI configuration, dependencies, generated files, or deployment configuration unless this prompt explicitly requests a documentation-only link or metadata correction.
2. Do not invent capabilities, guarantees, APIs, states, ownership boundaries, implementation status, security properties, protocol behaviour, or future commitments.
3. Treat accepted ADRs, exported public types, source code, tests, and the current README as evidence. In that order, prefer implemented and tested behaviour over prose.
4. If sources disagree, do not silently resolve the conflict. Add a clearly labelled "Documentation conflict" or "Open question" section and report it in the final summary.
5. Clearly distinguish: implemented and tested; implemented but not integration-tested; scaffolded; designed; proposed; explicitly out of scope.
6. Never describe proposed or scaffolded behaviour as currently working.
7. Do not add marketing language or unsupported claims such as "secure", "production-ready", "guaranteed", "zero trust", or "enterprise-grade".
8. Keep terminology consistent with project-runtime-contracts and accepted ecosystem terminology.
9. Use relative repository links.
10. Mermaid diagrams may be added only when every component, relationship, and transition is supported by repository evidence or an explicit requirement in this prompt.
11. Keep changes restricted to Markdown or plain-text documentation files and README documentation links.
12. Do not create empty placeholder files. Every created document must contain useful, repository-specific content.
13. Do not duplicate the README. New documentation should deepen, formalise, or operationalise material that is too detailed for the README.
14. Do not commit or push unless explicitly instructed. Prepare the changes and report the diff.

EVIDENCE AND WORKFLOW

Before editing:
- Inspect the repository tree.
- Read the README, existing docs, ADRs, public types, package boundaries, and relevant tests.
- List the evidence sources you will use.
- Identify contradictions, outdated statements, naming inconsistencies, and unresolved decisions.
- Confirm that the requested documents are not already present under another name.

During editing:
- Preserve existing accepted decisions.
- Prefer short normative statements for rules and detailed explanatory sections for rationale.
- Add links from the README or docs index only where useful.
- Use exact paths, type names, outcome names, and identifiers found in the repository.
- When referring to another repository, state clearly whether the dependency is current, optional, planned, or blocked.

After editing:
- Show every created and modified file.
- Summarise the purpose of each file.
- List every unresolved question or documentation conflict.
- List any wording that required inference rather than direct extraction.
- Run available Markdown formatting, lint, or link checks if already configured.
- Show `git diff --stat`.
- Show the documentation-only diff or a concise section-by-section summary.
- Confirm that no runtime code or configuration changed.


PROJECT: project-runtime-contracts
REPOSITORY: https://github.com/hourwise/Project-Adrasteia

PRIMARY GOAL
Treat documentation as part of the public protocol. Define semantics, ownership, compatibility, negotiation, evolution, and conformance without adding runtime logic or resolving domain decisions belonging to other projects.

AUTHORITY BOUNDARY
This repository owns shared interoperability shapes, vocabulary, protocol-version representation, accepted compatibility/evolution rules, and conformance fixtures/examples.
It does not own Ananke policy semantics, Mnemosyne reliability semantics, Horae orchestration policy, Moirae product behaviour, provider transport, or runtime logic.

REQUESTED DOCUMENTS
1. `docs/protocol-specification.md`
   For every public contract family document purpose, producer, consumer, semantic owner, required/optional fields, invariants, lifecycle, validation, unknown-value behaviour, examples, and compatibility implications.
2. `docs/version-negotiation.md`
   Define package versus protocol version, compatibility algorithm, supported ranges, capability/feature negotiation, unknown capabilities/enums, optional-field evolution, incompatibility representation, downgrade behaviour, and whether negotiation is symmetric or host-led.
3. `docs/evolution-policy.md`
   Define breaking versus additive changes, enum/union widening, optional/required field rules, rename/removal, deprecation, support window if decided, experimental namespaces, schema migration, and changelog requirements. Do not invent a support period.
4. `docs/contract-ownership.md`
   Map semantics to owners: action outcomes to Ananke, context packs to Mnemosyne, compositions to Horae, host/product behaviour to Moirae Code, shared representation to this repository. Verify every assignment.
5. `docs/conformance.md`
   Define evidence required for third-party compatibility: schema validation, negotiation, identifiers, unknown fields, outcome handling, serialization round trips, canonical examples, negative tests, and fixtures. Separate implemented tests from proposed requirements.
6. `docs/glossary.md`
   Establish authoritative definitions for runtime, host, capability, composition, proposal, approval, execution, outcome, audit event, context pack, protocol version, correlation identifier, connector, and model broker.
7. `CHANGELOG.md`
   Create only if absent. Add Unreleased and evidence-backed history only; do not fabricate releases.
8. `docs/decisions/README.md`
   Create/update ADR index.

CONSISTENCY CLEANUP
Inspect and correct Moira Code versus Moirae Code, duplicated current/future consumers, contradictory consumer claims, package/protocol terminology, stale examples, and renamed links. Do not change classifications without evidence.

SPECIAL REVIEW QUESTIONS
Do not silently decide: safety of new enum members; handling unknown union variants; directional compatibility; descriptive versus normative capabilities; correlation-ID generation; serialization order; ownership of canonicalisation; reliability representation; whether negotiation precedes discovery.

README CHANGES
Keep it concise: purpose, install/use summary, current consumers, and links to protocol/versioning/evolution/conformance/glossary/changelog.

DELIVERABLE STANDARD
A third-party implementer should be able to answer: What does each contract mean? Who owns that meaning? Which versions communicate? What changes break compatibility? What must be tested? How are unknown values handled?
