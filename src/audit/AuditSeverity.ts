/**
 * Re-exports for convenience.
 * AuditEvent uses the same Severity enum as RuntimeError for consistency.
 */
export { Severity } from "../results/Severity";
export { SeveritySchema } from "../results/Severity";

