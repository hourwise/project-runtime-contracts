/**
 * Default protocol constants.
 * Used as fallbacks and defaults when negotiating between runtimes.
 */

export const DEFAULT_PROTOCOL_VERSION = "1.0.0";
export const DEFAULT_MINIMUM_SUPPORTED_VERSION = "1.0.0";

